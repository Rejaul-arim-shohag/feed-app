# EAS pnpm install can block newly published packages via minimum-release-age policy.
# Temporarily disable age gate for CI/build until the cutoff window passes.
minimum-release-age=0

# Keep explicit excludes as a fallback for environments that enforce age policies.
minimum-release-age-exclude=@expo/image-utils,@expo/require-utils,expo-constants,expo-notifications
